<!-- What changed, and why. A sentence or two is fine. -->

**Base branch**

- [ ] Opened against `main`

**Checks**

- [ ] `npm test` passes
- [ ] `package.json` and `herdr-plugin.toml` versions still match
